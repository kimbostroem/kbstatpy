[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kbstatpy"
dynamic = ["version"]
description ="Generalised linear mixed model (GLMM) analysis with post-hoc comparisons, data transformation, correlation analysis, and multicollinearity diagnostics. Modelled after the MATLAB kbstat library."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Kim Joris Boström", email = "kim.bostroem@predimo.com" }]
dependencies = [
    "pymer4",
    "polars",
    "pyarrow",
    "pandas",
    "scipy",
    "rpy2",
    "scikit-learn",
    "formulae",
    "great-tables",
    "joblib",
    "numpy",
    "sympy",
    "matplotlib",
    "seaborn",
    "openpyxl",
    "mpld3",
]

# PyYAML is needed by tests/test_citation_metadata.py alone, which validates
# CITATION.cff as real YAML rather than by regex. It is deliberately not a
# runtime dependency: nothing in kbstatpy reads that file. Declaring it here
# rather than installing it ad hoc in CI keeps the requirement discoverable for
# anyone running the suite locally.
[project.optional-dependencies]
test = ["pyyaml"]

[tool.setuptools]
packages = ["kbstatpy"]

[tool.setuptools.package-data]
kbstatpy = ["fonts/*.otf", "fonts/*.txt"]

[tool.setuptools.dynamic]
version = { attr = "kbstatpy.__version__" }
